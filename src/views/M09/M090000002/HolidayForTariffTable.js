import React from 'react'
import { connect } from 'react-redux'

const HolidayForTariffTable = () => {
  return (
    <div >
      HolidayForTariffTable.js
    </div>
  )
}

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = {

}

export default connect(mapStateToProps, mapDispatchToProps)(HolidayForTariffTable)
